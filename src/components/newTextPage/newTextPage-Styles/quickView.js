const styles = theme => ({
    chapterList: {
        listStyle: "none",
        textAlign: "left",
        '& button': {
            color: 'white',
            textTransform: "capitalize",
            textAlign: "left",
            cursor: "pointer",
            display: "block"
        }
    }
})

export default styles;